dashboard:
  basePath: /

  replicaCount: 1

  ingress:
    enabled: true

    annotations:
      kubernetes.io/ingress.class: nginx
      kubernetes.io/tls-acme: "true"
    
    hosts:
    - host: hq.know-ops.com
      paths:
      - path: /
        type: ImplementationSpecific

    tls:
    - hosts:
      - hq.know-ops.com
      secretName: goldilocks-tls

